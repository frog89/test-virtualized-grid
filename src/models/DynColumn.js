class DynColumn {
  constructor(colIndex, dataKey, label, width) {
    this.colIndex = colIndex;
    this.dataKey = dataKey
    this.label = label;
    this.width = width;
  }
}

export default DynColumn;